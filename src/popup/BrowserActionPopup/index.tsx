import React = require( 'react' );

import AuthPage from 'popup/AuthPage';

export default class BrowserActionPopup extends React.PureComponent
{
  public render()
  {
    return (
      <AuthPage />
    );
  }
}
